
def in_bound(arr, i: int) -> bool:
    return i > -1 and i < len(arr)

def star_equals(arr, center: (int, int), equal_size: int):
    (x, y) = center
    width = len(arr[0])
    min_x_index = max(0, x - equal_size)
    max_x_index = min(width-1, x + equal_size)


    arr_to_check = arr[]
    return []
